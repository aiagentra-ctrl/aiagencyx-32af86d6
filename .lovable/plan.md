## E-commerce Chatbot & Landing Page — Fix Pass

Targeted fixes across RLS, chatbot UI, voice screen, landing page, and FAQ. No rebuild — surgical edits to existing files.

### 1. Follow-Ups RLS (migration)

Audit `pg_policies` for these tables and add missing `authenticated` policies (SELECT/INSERT/UPDATE/DELETE) + required GRANTs:

- `follow_up_sequences_templates`, `follow_up_steps`, `follow_up_enrollments`, `followup_events`, `followup_rules`, `followup_settings`

### 2. Product image URL hardening

- Migration: `UPDATE products SET image_url = 'https:' || image_url WHERE image_url LIKE '//%'`.
- `scrape-ecommerce-products`: normalize protocol-relative and relative URLs before insert.
- `ChatProductCard` (in `EcomChatShell.tsx`): add `onError` fallback to `/placeholder-product.png` (add asset in `public/`).
- Pass through raw `image_url` from `recommendations` JSON without stripping query params.

### 3. Chatbot Home Screen (`EcomChatShell.tsx`)

- Restyle "Ask a question" row to match "Start a live call" (dark card, 52px, chat icon + chevron).
- Wrap Ask / chip grid / Live call in container `gap-2.5 px-4`.
- New **Recent Conversation card**: query `chatbot_conversations` by `chatbot_id` + visitor session, show latest user message preview + relative time; click → Chats tab with that session loaded.
- Bottom nav active state: brand color icon+label, 2px brand underline, 150ms transition; inactive `white/40`.
- Standardize "Powered by Aiagentra" footer across all screens.
- Verify header wave SVG scales across viewports.

### 4. Chat Screen — Ask a Question flow

- Empty state: brand logo circle (52px) + business name + `"I know {product_count} products. Ask me anything."` + 2×2 starter grid. Starter labels generated by LLM based on scraped catalog (call `chatbot-conversation` with a `generate_starters` intent, cache in session).
- Chip group component: pill style per spec, hover=brand, selected=brand-filled, group disables after one click (track `selectedChipGroupId` in message state).
- Chips render below bot bubble aligned with avatar (matches PLIX layout).

### 5. Voice Screen (`VoiceCallView` in `EcomChatShell.tsx`)

Extend existing view:

- Animated orb (80px) with state-driven rings: idle glow, connecting single pulse, listening 3-ring ripple, speaking orb scale-pulse.
- Status text with icon + crossfade transitions between Connecting / Listening / You're speaking / AI responding / Call ended.
- Live transcript box (max-h 180, auto-scroll) with user (right, brand) vs AI (left, white) prefixed lines from `vapi.on('message')` transcript events.
- **Product mention detection**: on each assistant transcript line, match against `chatbot.products[].name`; if hit, render mini horizontal product card (32px img + name + price + View →).
- Bottom controls row: Mute (48px), End Call (64px red), Speaker (48px) with labels; muted states styled.
- Header back arrow shows confirm dialog "End this call?" before ending.
- On end: toast `Call ended · {N} minutes`.

### 6. E-commerce Landing Page (`EcommerceLandingPage.tsx`)

Rebuild sections in koushikflow.netlify.app structure (keep existing hero personalization + floating widget mount): check our i have alredy prove all image and youtuve vido link chek our syteam our extarct form website

Use the existing website **[koushikflow.netlify.app](http://koushikflow.netlify.app)** as the exact design and layout reference for the new e-commerce demo landing page.

The landing page should match the reference website as closely as possible in terms of:

- Overall design and visual style
- Section order and structure
- Typography and spacing
- Colors and backgrounds
- Images and visual elements
- YouTube video section
- Buttons and CTA placement
- Animations and responsiveness
- Desktop and mobile layouts

All copy uses `{{company}}` / `{{first_name}}` via `renderTemplate`. Floating widget stays bottom-right.

### 7. FAQ Tab (`EcomChatShell.tsx`)

- Query `knowledge_base_entries` where `content_type = 'faq'` or `source_url` matches policy/faq/shipping/returns.
- Group into buckets (Shipping & Delivery / Returns & Refunds / Sizing & Fit / Payment & Orders) by keyword matching on question text; ungrouped → "General".
- Accordion cards per spec (chevron rotate, 200ms height animation).
- Fallback: text-search KB for common terms if no FAQ content.
- Bottom actions: "Ask the AI" → Chats tab, "Call AI" → Voice view.

### 8. Verification

- Playwright: load `/demo/<humanfitcraft-slug>` → verify hero copy, open widget, click Ask a question → assert starter grid + product cards render with images, click voice → orb + transcript states, open FAQ → accordion groups.
- Check console/network for image 404s and RLS errors.

### Technical notes

- Files touched: `src/components/chatbot/unified/ecom/EcomChatShell.tsx`, `src/components/demo/ecommerce/EcommerceLandingPage.tsx`, `supabase/functions/scrape-ecommerce-products/index.ts`, `supabase/functions/chatbot-conversation/index.ts` (starter-generation intent), new `public/placeholder-product.png`.
- Two migrations: RLS policies + image URL backfill.
- No changes to backend RAG, VAPI wiring, or scraping platform detection.